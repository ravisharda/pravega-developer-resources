
# List all Helm releases in deployed state
$ helm list

# List all Helm releases in all states
$ helm list -all

# Install a release 
$ helm install <chart>

# Delete a release
$ helm delete <chart> --purge
